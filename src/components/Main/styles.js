import styled from 'styled-components';

export const MainContainer = styled.main`
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    height: 100vh;
    width: 100%;
    background: #cecece;
`;